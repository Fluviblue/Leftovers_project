def Search_Formulas(word):  # define (...)
    definition =
    Invalid = "This formula cannot be found in our dictionary."

    for pod in res.pods:
        Formula = gTTS(text=definition, lang=language, slow=False)
        if split_file == word:
            do_something_with(
        return myobj

    else:
            myobj.save("Invalid.mp3")
            os.system("Invalid.mp3")
